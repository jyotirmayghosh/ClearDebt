<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greentech.jyotirmay.cleardebt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>
        <activity
            android:name=".AddMoneyActivity"
            android:label="Add Money"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".CategoryActivity"
            android:label="Select Category" />
        <activity
            android:name=".AddTransactionActivity"
            android:label="Add Daily Transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".AddPayeeActivity"
            android:label="Add Beneficiary Transaction"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LenderActivity"
            android:label="Select Category" />
        <activity
            android:name=".StartActivity"
            android:theme="@style/AppThemeNoAction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CarouselActivity"
            android:theme="@style/AppThemeNoAction"></activity>
    </application>

</manifest>